[package]
name = "sql-fingerprint"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT OR Apache-2.0"

[dependencies]
regex = "1.13.1"

[dev-dependencies]
criterion = "0.7.0"

[[bench]]
name = "fingerprint"
harness = false
